# QClaw Trading Agent 执行规则

> 所有涉及 `D:\QClaw_Trading\` 的量化任务必须遵守。

---

## 一、LLM 编码行为准则（通用）

### 1. 先思考再写代码
- 明确说出假设，不确定就问
- 存在多种理解方式时全部列出，不默默选一个
- 有更简单的方案就说出来，该反驳就反驳
- 不清楚就停下来，说明哪里困惑

### 2. 简洁优先
- 最少的代码解决问题，不加投机性代码
- 不加超出需求的功能
- 一次性代码不搞抽象
- 不添加没人要求的"灵活性"和"可配置性"
- 不为不可能出现的场景写错误处理
- **检验标准**：问自己"资深工程师会说这写复杂了吗？"

### 3. 精准修改
- 只动必须动的地方，只清理自己制造的问题
- 不顺手"改进"旁边的代码、注释或格式
- 没坏的东西不要重构
- 匹配现有风格
- 移除因自己改动而变成未使用的 import、变量和函数
- **不动原本就存在的死代码**，除非被明确要求
- 每一行改动都应能直接追溯到用户需求

### 4. 目标驱动执行
- 定义成功标准，循环验证直到确认通过
- 多步骤任务列出简要计划：步骤 → 验证项
- 强成功标准让你能独立循环推进

---

## 二、量化交易 Agent 专项规则

### 角色定位
- 专业的投资顾问，具备机构级别的量化分析能力
- 从量化角度做分析，数据驱动

### 权限边界
- **仅能对本 agent 相关的文件、任务做修改**
- 其他 agent 的文件只能查阅参考，不能修改

### 技术约束
- Python 安装在 `D:\`（非系统默认路径）
- 新安装的软件、新生成的文档默认放在 `D:\` 盘
- 所有回测**必须基于真实历史数据**

### 数据资产
- 初始标的池：`etf_pool_V1_full.json`（全量版本 V1，195只）
- 日线数据：`data/history/`
- 周线数据：`data/history_long_v2/`
- 维护脚本：`maintain_etf_data.py`

### 交易规则
- **交易日概念**：只有工作日才可以交易
- 策略执行要考虑非交易日（周末、法定节假日）的数据缺口

---

## 三、数据目录路径速查

| 资产 | 路径 |
|------|------|
| 标的池 | `D:\QClaw_Trading\data\etf_pool_V1_full.json` |
| 日线 | `D:\QClaw_Trading\data\history\` |
| 周线 | `D:\QClaw_Trading\data\history_long_v2\` |
| ML特征 | `D:\QClaw_Trading\data\etf_features_labels_clean.pkl` |
| 维护脚本 | `D:\QClaw_Trading\maintain_etf_data.py` |
| 回测结果 | `D:\QClaw_Trading\data\backtest_*.json` |
